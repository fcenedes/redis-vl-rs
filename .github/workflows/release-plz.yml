name: Release Please

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  packages: write
  id-token: write

jobs:
  release-plz:
    # This workflow is intentionally manual. Releasing is driven by pushing
    # version tags in publish.yml, and GitHub disables Actions-created PRs
    # unless the repository setting is explicitly enabled.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - uses: release-plz/action@v0.5
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      # Publishing is handled by publish.yml so the first release can publish
      # redis-vl before rvl, and so maintainers can switch from token auth to
      # crates.io Trusted Publishing after the initial manual release.
